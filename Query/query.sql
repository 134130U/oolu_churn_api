select account_id, account_number, status, arrhes_day,
       price_program, arrhes, total_amount, minimum_payment, gap,
       invoce_model, created_at, total_payed, next_payment,balance,
       group_name, prev_payment, prev_account_number, slug,
       cutoff_days, register_by, day_disabled, written_off,
       written_off_at, expected_total_amount, repossessed_at,id_tenant, commune,
       region
	   from account_analytics